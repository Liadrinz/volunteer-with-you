<view>
    <cu-custom CustomBar="height:4rem;" bgColor="bg-gradual-red" isBack="{{true}}">
        <view slot="backText">返回</view>
        <view slot="content">进行中活动</view>
    </cu-custom>
    <activity showFloatBtn="{{false}}" actType="bv" activityList="{{doingAct}}" />
</view>
