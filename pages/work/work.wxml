<view>
    <view style="margin-top: 4rem;">
        <!-- <publish id="publish" wx:if="{{tabCur === 0}}" /> -->
        <view>
            <activity id="acts" actType="work" showFloatBtn="{{showFloatBtn}}" activityList="{{activityList}}" />

        </view>
    </view>
</view>
